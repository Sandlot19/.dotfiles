. $HOME/.zsh.path
source $HOME/.zsh.exports

# To be provided locally and not copied to dotfiles. The if prevents annoying shell warnings being
# printed if it doesn't exist. This goes first so any updates to $PATH are appended from here before
# anything else.
if [ -f "$HOME/.zsh.private" ] ; then
  . $HOME/.zsh.private
fi

# Set up fzf key bindings and fuzzy completion. Fzf must be installed and in $PATH.
source <(fzf --zsh)
